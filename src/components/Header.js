import React from "react";
import ReactDOM from "react-dom";

function Header() {
    return (
        <nav class="navbar">
            <div class="container-fluid">
                <span class="navbar-brand m-2 h1">MediTime</span>
            </div>
        </nav>
    );

}

export default Header;
